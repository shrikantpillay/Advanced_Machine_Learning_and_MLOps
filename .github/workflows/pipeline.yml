name: Advanced_Machine_Learning_and_MLOps

on:
  workflow_dispatch:

jobs:
  debug-repository:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # This step will print your entire project structure to the logs
      - name: List All Files in Repository
        run: |
          echo "=== CURRENT DIRECTORY ==="
          pwd
          echo "=== ALL FILES AND FOLDERS ==="
          ls -R
